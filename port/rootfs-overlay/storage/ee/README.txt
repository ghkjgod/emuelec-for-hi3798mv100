HiSTB EmuELEC persistent state lives here initially.

Create an empty file named disable-autostart to keep the frontend from
starting after boot.  Application-only releases may be installed below
releases/ and selected by the current symlink.  No script in this image
formats or writes the cadata or others partitions automatically.
